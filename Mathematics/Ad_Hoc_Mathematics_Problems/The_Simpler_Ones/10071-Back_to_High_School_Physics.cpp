#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	short a,b;

	while(cin >> a >> b)
		cout << 2*a*b << endl;
	
	return 0;
}